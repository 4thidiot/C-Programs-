#include <stdio.h>

int summation(int);
int main() {
	
	int n;
	scanf("%d", &n);
	printf ("%d", summation(n));

}

int summation(int n)
{
    if(n==0)
     return 0;
    else
     return n+summation(n-1);
}
